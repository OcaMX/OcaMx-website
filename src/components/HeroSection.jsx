import React from "react";

const HeroSection = () => {
  return (
    <section id="herosection" className="min-h-screen flex items-center justify-center">
      <h2 className="text-3xl">HeroSection Section</h2>
    </section>
  );
};

export default HeroSection;
